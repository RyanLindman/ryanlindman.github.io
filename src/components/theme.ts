import { createTheme } from '@mui/material'

export const globalTheme = createTheme({
  palette: {
    primary: {
      main: '#8F2EB2',
      light: '#e3f2fd',
    },
    secondary: {
      main: '#7C279A',
      light: '#F5EBFF',
    },
  },
})
